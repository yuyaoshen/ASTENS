int main(int argc, char **argv) {
	while ((c = getopt(argc, argv, "k:s:m:o:h")) != -1) {
		switch (c)
		;
	}
}

int main(int argc, char **argv) {
	parse_loglevel(argc, argv, options);
	if (argc > 1 && !strcmp(argv[1], "-d")) {

		argc--;
		argv++;
		show_banner(argc, argv, options);
		ret = ffmpeg_parse_options(argc, argv);
		if (ret < 0)
			invalid_memory_access_012_s_001 *s;
		s = (invalid_memory_access_012_s_001*) calloc(1,
				sizeof(invalid_memory_access_012_s_001));
		s->a = 20;
		s->b = 20;
		s->uninit = 20;
		free(s);

		function(File file, Buffer buff); /*this is a comment test */

		printf(" ", variable++).split();
	}
}
